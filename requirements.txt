# Dependencies for Rapor Scanner App
# Python 3.9+ required
# Updated: 2026-01-03

# Core
streamlit>=1.28.0
pandas>=2.0.0

# PDF Processing
pdfplumber>=0.10.0
pdf2image>=1.16.0

# OCR (Experimental - for scanned PDFs)
easyocr>=1.7.0
# Torch CPU-only (tanpa CUDA, lebih kecil)
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# Image Processing
Pillow>=10.0.0
numpy>=1.24.0

# Excel Export
openpyxl>=3.1.0

# System Dependencies (install separately):
# - poppler-utils (for pdf2image)
#   Ubuntu/Debian: apt install poppler-utils
#   macOS: brew install poppler
